fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
pydantic>=2.7.0

# Speech-to-text (install separately if needed)
openai-whisper>=20231117

# TTS options (install one or more)
# kokoro>=0.9.4
# soundfile>=0.12.1
# pyttsx3>=2.90

# Optional: for numpy (required by whisper + soundfile)
numpy>=1.24.0
